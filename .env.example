# ─────────────────────────────────────────────────────────────
# GTAS VPP – Environment Configuration Template
# Copy to .env and fill in production values:  cp .env.example .env
# Domain: gtas-vpp.annam.id.vn
# ─────────────────────────────────────────────────────────────

# ── Radzen Blazor MCP ────────────────────────────────────────
# Get free trial (50 requests/15 days): https://www.radzen.com/blazor-mcp/documentation/get-your-key
RADZEN_API_KEY=your-radzen-api-key-here

# ── Database ─────────────────────────────────────────────────
# Database name: GTAS_VPP_LIVE (cả TestEnv + LiveEnv trỏ cùng DB này)
DB_SA_PASSWORD=YourStrong!Passw0rd
MSSQL_MEMORY_LIMIT_MB=4096

# ── JWT Authentication (tối thiểu 32 ký tự) ─────────────────
# Tạo key ngẫu nhiên: openssl rand -base64 48
JWT_KEY=change-this-to-a-strong-random-key-min-32-chars

# ── TripleDES password encryption (legacy compat) ───────────
# Default "ttpsolutions" tương thích DB user shared GTAS_MENU.
# Khi triển khai DB user thật trên server công ty, đổi key này
# thành key tương ứng với tblUsers.PasswordChar đang lưu.
PASSWORD_ENCRYPTION_KEY=ttpsolutions
