# DEP-001 — CI and DigitalOcean production deployment

- Status: DONE — production validated
- Branch: `Nam`
- Deployed source merge commit: `b681c5565723d7b977a5a4b4963785efbdbb2506`
- Date: 2026-07-17 (Asia/Ho_Chi_Minh)

## Evidence

- PR #4 (`10a15ccf`) merged the release evidence/CI reconciliation slice.
- The first post-merge deploy run `29553273729` correctly stopped before
  rollout because the frontend Docker publish step used `--no-restore` after a
  BuildKit NuGet cache mount. No production rollout occurred in that failed
  attempt.
- PR #5 (`b681c556`) removed the unsafe frontend `--no-restore` assumption.
  Both push and pull-request CI runs passed secret scan, build/test, EF model,
  NuGet vulnerability and final DOCX gates.
- DigitalOcean workflow run `29553512300` passed all three jobs:
  `verify`, `build-and-push`, and `deploy`.
- The deploy job verified the production environment file, uploaded the
  immutable release bundle, rolled out GHCR images tagged with the deployed
  commit, confirmed healthy backend/frontend containers, rechecked key-only
  SSH access, and passed the public boundary check.
- The public `https://gtas-vpp.annam.id.vn/healthz` endpoint returned `200
  Healthy` from the local workspace after the workflow completed.

## Boundary

This proves the application deployment and public health boundary for the
release. It does not claim a production database backup/restore rehearsal,
real-provider email deliverability or revocation of the eight historical
Google-key alerts recorded under SEC-001.
