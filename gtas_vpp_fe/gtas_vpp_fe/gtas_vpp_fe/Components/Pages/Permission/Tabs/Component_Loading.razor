@using Microsoft.AspNetCore.Components

@if (isActive)
{
    <RadzenProgressBar Mode="ProgressBarMode.Indeterminate" ShowValue="false" Style="height: 4px" />
}

@code {
    [Parameter] public bool isActive { get; set; }
}
